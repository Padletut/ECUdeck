# Legacy

This directory keeps older implementation experiments during the stack migration.

Current contents:

- `python_backend/`: old parsing and analysis experiments retained only as migration reference

Nothing in this directory should define the long-term ECUDeck architecture.
